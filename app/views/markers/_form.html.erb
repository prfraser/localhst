<%= form_for([@marker.tour, @marker], :class => "form-horizontal col-sm-6") do |form| %>
  <% if @marker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@marker.errors.count, "error") %> prohibited this @marker from being saved:</h2>

      <ul>
      <% @marker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="col-sm-12">
      <%= form.text_field :name, id: :marker_name, placeholder: "Marker name", :class => "form-control" %>
    </div>
  </div>
  
  <div class="form-group">
    <div class="col-sm-12">
      <%= form.text_field :address, id: :marker_address, placeholder: "Marker address", :class => "form-control" %>
    </div>
  </div>

  <div class="form-group col-sm-12">
    <%= form.submit class: "btn btn-info" %>
  </div>
<% end %>
